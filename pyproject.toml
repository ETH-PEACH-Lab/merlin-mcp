[project]
name = "merlin-mcp"
version = "0.1.0"
description = "Metadata and Dependecies for the Merlin MCP"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.26.0",
    "pydantic>=2.12.0",
]
